<Page x:Class="TestTaskWPF.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TestTaskWPF.Pages" 
      xmlns:viewmodels="clr-namespace:TestTaskWPF.ViewModels"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage"
      d:DataContext="{d:DesignInstance Type=viewmodels:CoinViewModel}" 
      Loaded="Page_Loaded">
    
    <Grid>
        <ListView ItemsSource="{Binding Coins}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Ранг" DisplayMemberBinding="{Binding Rank}" Width="50"/>
                    <GridViewColumn Header="Символ" DisplayMemberBinding="{Binding Symbol}" Width="100"/>
                    <GridViewColumn Header="Ціна (USD)" DisplayMemberBinding="{Binding PriceUsd}" Width="150"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
